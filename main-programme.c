#include <stdio.h>
int main(){

    FILE *fptr;

    printf("please enter a directory:");

    for(int i=NULL; (i = getchar()) != EOF ;)
        scanf("%d", &i);


    fptr = fopen("", "r");






    return 0;
}
